---
title: "{% if title|length > title_length %}{{ title|slice(0,title_length)|raw }}...{% else %}{{ title|raw }}{% endif %}"
date: {{ date }}
icon: article
source: Wallabag
domains:
- {{ host }}
src_url: "{{ wallabag_url }}"
{{ year }}: "{{ month }}"
categories:
{% for tag in tags %}
    - {{ tag }}
{% endfor %}
---
Het oorspronkelijke artikel kan je lezen op [{{ host }}]({{ original_url }}) of [op Wallabag]({{ wallabag_url }}). De leestijd is ongeveer {{ reading_time }} {% if reading_time != 1 %}minuten{% else %}minuut{% endif %}.