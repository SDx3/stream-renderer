---
title: "{% if title|length > title_length %}{{ title|slice(0,title_length)|raw }}...{% else %}{{ title|raw }}{% endif %}"
date: {{ date }}
icon: bookmark
source: Firefox bookmarks
src_url: {{ url }}
categories:
{% for category in categories %}
- {{ category }}
{% endfor %}

---
Deze bookmark kan je vinden op [{{ host }}]({{ url }}).
